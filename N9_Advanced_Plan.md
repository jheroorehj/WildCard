# N9 Learning Pattern Analyzer 고도화 계획

## 목표
단순 텍스트 리스트 형태의 JSON → UI 시각화(레이더 차트) + 행동경제학 기반 진단 + 구체적 행동 유도(Action Item) 구조로 전면 개편

**핵심 방향**: 사용자 친화적 축 이름 + 내부 분석에 행동경제학 개념 매핑 (하이브리드 방식)
- 단순 "분석 깊이 부족" → **"확증 편향(듣고 싶은 것만 듣는 성향)이 강해요"**
- 서비스 신뢰도와 몰입감 극대화

## 현재 구조 (완전 대체 예정)
```json
{
  "learning_pattern_analysis": {
    "pattern_summary": "string",
    "pattern_strengths": ["string"],
    "pattern_weaknesses": ["string"],
    "learning_recommendation": {...},
    "uncertainty_level": "low|medium|high"
  }
}
```

## 새로운 구조 (행동경제학 기반)

```json
{
  "learning_pattern_analysis": {
    "investor_character": {
      "type": "확증 편향형 수집가",
      "description": "다양한 정보를 수집하지만, 자신의 기존 믿음을 확인하는 정보에 더 큰 가중치를 두는 경향이 있습니다.",
      "behavioral_bias": "confirmation_bias"
    },

    "profile_metrics": {
      "information_sensitivity": {
        "score": 85,
        "label": "정보 민감도",
        "bias_detected": "정보 과부하(Information Overload)"
      },
      "analysis_depth": {
        "score": 45,
        "label": "분석 깊이",
        "bias_detected": "확증 편향(Confirmation Bias)"
      },
      "risk_management": {
        "score": 30,
        "label": "리스크 관리",
        "bias_detected": "손실 회피(Loss Aversion)"
      },
      "decisiveness": {
        "score": 55,
        "label": "결단력",
        "bias_detected": "현상 유지 편향(Status Quo Bias)"
      },
      "emotional_control": {
        "score": 40,
        "label": "감정 통제",
        "bias_detected": "군중 심리(Herding Effect)"
      },
      "learning_adaptability": {
        "score": 70,
        "label": "학습 적응력",
        "bias_detected": null
      }
    },

    "cognitive_analysis": {
      "primary_bias": {
        "name": "확증 편향",
        "english": "Confirmation Bias",
        "description": "자신이 믿고 싶은 정보만 선택적으로 수용하고, 반대 증거는 무시하는 경향",
        "impact": "잘못된 투자 판단을 정당화하며 손실을 키울 수 있습니다"
      },
      "secondary_biases": [
        {
          "name": "후견 편향",
          "english": "Hindsight Bias",
          "description": "결과를 알고 난 후 '그럴 줄 알았다'고 착각하는 경향"
        }
      ]
    },

    "decision_problems": [
      {
        "problem_type": "뇌동매매",
        "psychological_trigger": "FOMO(Fear Of Missing Out)",
        "situation": "급등 뉴스를 접했을 때",
        "thought_pattern": "'지금 안 사면 영원히 기회를 놓친다'는 생각",
        "consequence": "고점 매수 후 하락 시 큰 손실",
        "frequency": "high"
      }
    ],

    "action_missions": [
      {
        "mission_id": "M001",
        "priority": 1,
        "title": "반대 의견 3개 찾기",
        "description": "매수하려는 종목에 대해 부정적인 의견이나 리스크 요인을 3가지 이상 찾아 기록하세요.",
        "behavioral_target": "확증 편향 완화",
        "expected_outcome": "균형 잡힌 정보 수집, 편향된 판단 감소",
        "difficulty": "medium",
        "estimated_impact": "high"
      }
    ],

    "uncertainty_level": "medium"
  }
}
```

## 수정 대상 파일

### 1. [prompt.py](N9_Learning_Pattern_Analyzer/prompt.py)
- `NODE9_SYSTEM_PROMPT` 전면 개편
- 행동경제학 프레임워크 기반 분석 지침 추가
- 정량적 수치(0~100) 산출 기준 명시
- 주요 인지 편향 목록 및 매핑 가이드 제공

### 2. [n9.py](N9_Learning_Pattern_Analyzer/n9.py)
- `_fallback()` 함수를 새 구조에 맞게 수정

### 3. [validator.py](utils/validator.py)
- `validate_node9()` 함수를 새 구조에 맞게 수정
- 각 필드의 타입 및 범위 검증 로직 추가 (score: 0~100 등)

---

## 상세 필드 정의

### investor_character (투자자 캐릭터)
| 필드 | 타입 | 설명 |
|------|------|------|
| type | string | 행동경제학 기반 캐릭터명 (예: "확증 편향형 수집가", "손실 회피형 투자자") |
| description | string | 해당 캐릭터 유형에 대한 2~3문장 설명 (심리적 특성 포함) |
| behavioral_bias | string | 대표 인지 편향 코드 (예: confirmation_bias, loss_aversion) |

### profile_metrics (레이더 차트용 6축 지표)
| 축 키 | 라벨 | 관련 편향 예시 | 범위 |
|-------|------|----------------|------|
| information_sensitivity | 정보 민감도 | 정보 과부하, 가용성 휴리스틱 | 0~100 |
| analysis_depth | 분석 깊이 | 확증 편향, 앵커링 효과 | 0~100 |
| risk_management | 리스크 관리 | 손실 회피, 처분 효과 | 0~100 |
| decisiveness | 결단력 | 현상 유지 편향, 후회 회피 | 0~100 |
| emotional_control | 감정 통제 | 군중 심리, FOMO | 0~100 |
| learning_adaptability | 학습 적응력 | 후견 편향, 자기과신 | 0~100 |

### cognitive_analysis (인지 편향 분석)
| 필드 | 타입 | 설명 |
|------|------|------|
| primary_bias | object | 가장 두드러진 편향 (name, english, description, impact) |
| secondary_biases | array | 보조 편향 목록 (name, english, description) |

### decision_problems (의사결정 문제점)
| 필드 | 타입 | 설명 |
|------|------|------|
| problem_type | string | 문제 유형 (뇌동매매, 손절 지연, 물타기 등) |
| psychological_trigger | string | 심리적 트리거 (FOMO, 손실 회피, 앵커링 등) |
| situation | string | 문제가 발생하는 구체적 상황 |
| thought_pattern | string | 그 순간의 사고 패턴 (예: "지금 안 사면...") |
| consequence | string | 예상되는 결과/피해 |
| frequency | string | 발생 빈도 (low/medium/high) |

### action_missions (실행 미션)
| 필드 | 타입 | 설명 |
|------|------|------|
| mission_id | string | 고유 식별자 (M001, M002...) |
| priority | int | 우선순위 (1이 가장 높음, 1~3) |
| title | string | 미션 제목 (행동 동사로 시작) |
| description | string | 구체적인 실행 방법 |
| behavioral_target | string | 목표하는 편향 완화 (예: "확증 편향 완화") |
| expected_outcome | string | 기대 효과 |
| difficulty | string | 난이도 (easy/medium/hard) |
| estimated_impact | string | 예상 효과 크기 (low/medium/high) |

---

## 프롬프트에 포함할 행동경제학 개념 매핑

```
# 인지 편향 사전 (프롬프트 내 참조용)

1. 확증 편향 (Confirmation Bias)
   - 신호: 긍정적 뉴스만 언급, 리스크 무시
   - 진단: "듣고 싶은 것만 듣는 성향"

2. 손실 회피 (Loss Aversion)
   - 신호: 손절 거부, 물타기 언급
   - 진단: "손실을 인정하기 어려워하는 성향"

3. 가용성 휴리스틱 (Availability Heuristic)
   - 신호: 최근 사건에 과도한 가중치
   - 진단: "눈에 띄는 정보에 과하게 반응"

4. 앵커링 효과 (Anchoring Effect)
   - 신호: 특정 가격대에 집착
   - 진단: "처음 본 숫자에 판단이 고정"

5. 군중 심리 (Herding Effect)
   - 신호: "다들 산다", SNS 언급
   - 진단: "남들 따라 움직이는 성향"

6. FOMO (Fear Of Missing Out)
   - 신호: "지금 안 사면", 급등 추격
   - 진단: "기회를 놓칠까 봐 조급해하는 성향"

7. 자기과신 (Overconfidence)
   - 신호: 과거 성공 강조, 리스크 과소평가
   - 진단: "자신의 판단을 과신하는 성향"

8. 처분 효과 (Disposition Effect)
   - 신호: 수익 빨리 실현, 손실 보유
   - 진단: "이익은 빨리, 손실은 늦게 정리"
```

---

## 검증 방법
1. **단위 테스트**: `validate_node9()` 함수에 새 구조의 샘플 데이터 입력
2. **LLM 출력 검증**: `node9_learning_pattern_analyzer()` 호출 후 JSON 구조 확인
3. **프론트엔드 연동**: `profile_metrics` 내 각 축의 `score` 값으로 레이더 차트 렌더링 가능 여부 확인

---

## 프론트엔드 고도화 전략

### 디자인 톤앤매너 유지 원칙

**현재 디자인 시스템 분석:**
- **배경**: `bg-slate-950` (다크 테마), `bg-slate-900/40` (카드)
- **N9 섹션 색상**: Emerald (`border-emerald-500/20`, `text-emerald-400`)
- **카드 스타일**: `rounded-3xl`, 반투명 배경 + 미묘한 border
- **타이포**: Inter + Noto Sans KR, 작은 사이즈(`text-[11px]`), `font-black` 헤드라인
- **애니메이션**: `fade-in`, `slide-in-from-bottom-4`

**유지해야 할 핵심 요소:**
1. Emerald 색상 팔레트 (N9 정체성)
2. 반투명 카드 + 점 그리드 배경 패턴
3. 확장/축소 인터랙션 패턴
4. 작고 밀도 높은 정보 표현 방식

---

### 새로운 UI 컴포넌트 설계

#### 1. 투자자 페르소나 카드 (investor_character)

```
┌─────────────────────────────────────────────────┐
│  🧠  나의 투자 성향                              │
├─────────────────────────────────────────────────┤
│                                                 │
│     ┌───────────────────────┐                   │
│     │   확증 편향형 수집가    │  ← 캐릭터 타입    │
│     └───────────────────────┘                   │
│                                                 │
│  "다양한 정보를 수집하지만, 자신의 기존 믿음을    │
│   확인하는 정보에 더 큰 가중치를 두는 경향"       │
│                                                 │
│  ┌─────────────────────────────┐                │
│  │ #confirmation_bias          │ ← 배지        │
│  └─────────────────────────────┘                │
└─────────────────────────────────────────────────┘
```

**스타일링:**
```tsx
// 캐릭터 타입 - 강조된 라벨 스타일
className="inline-block px-4 py-2 bg-emerald-500/20 border border-emerald-500/40
  rounded-2xl text-emerald-300 text-lg font-black tracking-tight"

// 설명 텍스트
className="text-[12px] text-slate-300 leading-relaxed mt-3"

// 편향 배지
className="inline-flex items-center gap-1.5 px-2.5 py-1 bg-slate-800/60
  rounded-full text-[10px] text-emerald-400/80 font-mono"
```

---

#### 2. 레이더 차트 (profile_metrics) - CSS 기반 구현

**차트 라이브러리 없이 SVG + CSS로 구현** (기존 시스템 일관성 유지)

```
              정보 민감도
                 85
                  ▲
                 ╱ ╲
    학습 적응력 ╱   ╲ 분석 깊이
         70   ╱     ╲   45
              ╲     ╱
               ╲   ╱
        감정 통제 ╲╱ 결단력
           40    55

              리스크 관리
                 30
```

**구현 방식:**
```tsx
// SVG 기반 육각형 레이더 차트
<svg viewBox="0 0 200 200" className="w-48 h-48">
  {/* 배경 육각형 그리드 (20%, 40%, 60%, 80%, 100%) */}
  {[0.2, 0.4, 0.6, 0.8, 1].map(scale => (
    <polygon
      key={scale}
      points={hexagonPoints(scale)}
      className="fill-none stroke-slate-700/50 stroke-[0.5]"
    />
  ))}

  {/* 데이터 영역 */}
  <polygon
    points={dataPoints}
    className="fill-emerald-500/20 stroke-emerald-400 stroke-2"
  />

  {/* 각 축 라벨 */}
  {metrics.map((m, i) => (
    <text
      x={labelPositions[i].x}
      y={labelPositions[i].y}
      className="fill-slate-400 text-[8px] font-medium"
    >
      {m.label}
    </text>
  ))}
</svg>
```

**불균형 시각화:**
- 점수 30 이하: 해당 축 방향으로 움푹 들어간 형태
- 점수 70 이상: 해당 축 방향으로 뾰족하게 튀어나온 형태
- 전체적으로 비대칭한 육각형 → 사용자의 불균형 패턴 직관적 인지

---

#### 3. 인지 편향 분석 카드 (cognitive_analysis)

```
┌─────────────────────────────────────────────────┐
│  ⚠️  감지된 인지 편향                            │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─ 주요 편향 ─────────────────────────────────┐│
│  │                                             ││
│  │  확증 편향  Confirmation Bias               ││
│  │  ─────────────────────────────────          ││
│  │  자신이 믿고 싶은 정보만 선택적으로 수용하고,  ││
│  │  반대 증거는 무시하는 경향                    ││
│  │                                             ││
│  │  💥 영향: 잘못된 투자 판단을 정당화하며       ││
│  │          손실을 키울 수 있습니다              ││
│  └─────────────────────────────────────────────┘│
│                                                 │
│  ┌─ 보조 편향 ─────────────────────────────────┐│
│  │  · 후견 편향 (Hindsight Bias)               ││
│  │    결과를 알고 난 후 '그럴 줄 알았다'고 착각  ││
│  └─────────────────────────────────────────────┘│
└─────────────────────────────────────────────────┘
```

**스타일링:**
```tsx
// 주요 편향 카드 - 강조 스타일
className="bg-amber-500/10 border border-amber-500/30 rounded-2xl p-4"

// 편향 이름 (한글 + 영문)
<span className="text-amber-300 font-bold text-sm">{name}</span>
<span className="text-amber-400/60 text-[10px] font-mono ml-2">{english}</span>

// 보조 편향 - 리스트 스타일
className="bg-slate-800/40 border border-slate-700/50 rounded-xl p-3 mt-3"
```

---

#### 4. 의사결정 문제점 카드 (decision_problems)

```
┌─────────────────────────────────────────────────┐
│  🎯  의사결정 패턴 분석                          │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─────────────────────────────────────────────┐│
│  │  뇌동매매                     빈도: ●●●     ││
│  │  ─────────────────────────────────          ││
│  │                                             ││
│  │  😰 심리적 트리거                            ││
│  │  FOMO (Fear Of Missing Out)                 ││
│  │                                             ││
│  │  📍 발생 상황                                ││
│  │  급등 뉴스를 접했을 때                        ││
│  │                                             ││
│  │  💭 그 순간의 생각                            ││
│  │  "지금 안 사면 영원히 기회를 놓친다"          ││
│  │                                             ││
│  │  ⚡ 결과                                     ││
│  │  고점 매수 후 하락 시 큰 손실                 ││
│  └─────────────────────────────────────────────┘│
└─────────────────────────────────────────────────┘
```

**빈도 표시 (frequency):**
```tsx
// 빈도 인디케이터
const FrequencyIndicator = ({ level }: { level: 'low' | 'medium' | 'high' }) => (
  <div className="flex gap-0.5">
    {[0, 1, 2].map(i => (
      <div
        key={i}
        className={`w-2 h-2 rounded-full ${
          i < { low: 1, medium: 2, high: 3 }[level]
            ? 'bg-rose-400'
            : 'bg-slate-700'
        }`}
      />
    ))}
  </div>
)
```

---

#### 5. 액션 미션 카드 (action_missions)

```
┌─────────────────────────────────────────────────┐
│  🚀  오늘의 투자 미션                            │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─ M001 ──────────────────────────────────────┐│
│  │                                             ││
│  │  #1 우선순위                                 ││
│  │                                             ││
│  │  📋 반대 의견 3개 찾기                        ││
│  │  ─────────────────────────────────          ││
│  │  매수하려는 종목에 대해 부정적인 의견이나      ││
│  │  리스크 요인을 3가지 이상 찾아 기록하세요.     ││
│  │                                             ││
│  │  ┌─────────────────────────────────────┐    ││
│  │  │ 🎯 목표: 확증 편향 완화               │    ││
│  │  │ ✨ 기대효과: 균형 잡힌 정보 수집      │    ││
│  │  └─────────────────────────────────────┘    ││
│  │                                             ││
│  │  ┌──────────┐  ┌──────────┐                 ││
│  │  │ 난이도 중 │  │ 효과 높음 │                 ││
│  │  └──────────┘  └──────────┘                 ││
│  │                                             ││
│  │  [ ✓ 미션 시작하기 ]                         ││
│  └─────────────────────────────────────────────┘│
└─────────────────────────────────────────────────┘
```

**난이도/효과 배지:**
```tsx
const DifficultyBadge = ({ level }: { level: 'easy' | 'medium' | 'hard' }) => {
  const styles = {
    easy: 'bg-green-500/20 text-green-400 border-green-500/30',
    medium: 'bg-amber-500/20 text-amber-400 border-amber-500/30',
    hard: 'bg-rose-500/20 text-rose-400 border-rose-500/30'
  }
  const labels = { easy: '쉬움', medium: '보통', hard: '어려움' }

  return (
    <span className={`px-2 py-0.5 rounded-full text-[9px] font-bold border ${styles[level]}`}>
      난이도 {labels[level]}
    </span>
  )
}
```

---

### 수정 대상 프론트엔드 파일

| 파일 | 수정 내용 |
|------|----------|
| [types.ts](frontend/types.ts) | `N9LearningPatternResponse` 타입 전면 개편 |
| [AnalysisView.tsx](frontend/components/AnalysisView.tsx) | N9 섹션 (라인 454-648) 전면 개편 |
| [constants.tsx](frontend/components/constants.tsx) | N9 관련 아이콘/메타데이터 추가 |

---

### 새로 추가할 컴포넌트

```
frontend/components/
├── N9/
│   ├── InvestorPersonaCard.tsx    # 투자자 캐릭터 카드
│   ├── RadarChart.tsx             # SVG 기반 레이더 차트
│   ├── CognitiveBiasCard.tsx      # 인지 편향 분석 카드
│   ├── DecisionProblemCard.tsx    # 의사결정 문제점 카드
│   ├── ActionMissionCard.tsx      # 액션 미션 카드
│   └── index.ts                   # 배럴 파일
```

---

### 레이아웃 구조

```
┌─────────────────────────────────────────────────────────┐
│  투자 패턴 분석 (N9)                           [확장 ▼] │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────┐  ┌─────────────────────────┐   │
│  │                     │  │                         │   │
│  │   🧠 투자자 페르소나  │  │   📊 레이더 차트        │   │
│  │                     │  │       (6축 지표)        │   │
│  │   확증 편향형 수집가   │  │                         │   │
│  │                     │  │         /\              │   │
│  │   설명...           │  │        /  \             │   │
│  │                     │  │       /    \            │   │
│  └─────────────────────┘  └─────────────────────────┘   │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  ⚠️ 감지된 인지 편향                               │  │
│  │  ───────────────────────────────────────────────  │  │
│  │  주요: 확증 편향 | 보조: 후견 편향                  │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  🎯 의사결정 문제점                                │  │
│  │  ───────────────────────────────────────────────  │  │
│  │  뇌동매매 · FOMO · 급등 뉴스 상황                   │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  🚀 오늘의 투자 미션                               │  │
│  │  ───────────────────────────────────────────────  │  │
│  │  M001: 반대 의견 3개 찾기          [ 시작하기 ]    │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

### 애니메이션 & 인터랙션

**1. 레이더 차트 애니메이션:**
```tsx
// 초기 로드 시 중심에서 확장
<polygon
  className="animate-[radar-expand_0.8s_ease-out_forwards]"
  style={{ transformOrigin: 'center' }}
/>

// @keyframes radar-expand
// from { transform: scale(0); opacity: 0; }
// to { transform: scale(1); opacity: 1; }
```

**2. 미션 카드 체크 애니메이션:**
```tsx
// 완료 시 체크마크 + 색상 변경
className={`transition-all duration-300 ${
  completed
    ? 'bg-emerald-500/20 border-emerald-500/40'
    : 'bg-slate-900/40 border-slate-700/50'
}`}
```

**3. 편향 탐지 시 하이라이트:**
```tsx
// bias_detected가 있는 축은 펄스 효과
<circle
  className={bias ? 'animate-pulse' : ''}
  fill={bias ? '#f59e0b' : '#10b981'}
/>
```

---

## 구현 순서

### Phase 1: 백엔드
1. [prompt.py](N9_Learning_Pattern_Analyzer/prompt.py) - NODE9_SYSTEM_PROMPT 개편
2. [validator.py](utils/validator.py) - validate_node9() 수정
3. [n9.py](N9_Learning_Pattern_Analyzer/n9.py) - _fallback() 수정

### Phase 2: 프론트엔드
4. [types.ts](frontend/types.ts) - 타입 정의 업데이트
5. N9 컴포넌트 폴더 생성 및 개별 컴포넌트 구현
6. [AnalysisView.tsx](frontend/components/AnalysisView.tsx) - N9 섹션 교체
7. 애니메이션 및 인터랙션 추가

### Phase 3: 통합 테스트
8. API 응답 → 프론트엔드 렌더링 E2E 테스트
9. 레이더 차트 정상 렌더링 확인
10. 반응형 레이아웃 검증
