# N10 Learning Tutor 행동경제학 고도화 계획

## 목표
단순 "정보 전달자"에서 **"투자 행동 교정 코치"**로 진화

**핵심 방향**: 4가지 행동경제학 이론을 적용하여 실제 행동 변화를 유도하는 넛지(Nudge) 설계
- **구현 의도 (Implementation Intentions)**: If-Then 플랜으로 실행 확률 2-3배 향상
- **프레이밍 효과 (Framing Effect)**: 손실을 "수업료"로 재정의하여 방어기제 완화
- **사후 가정 사고 (Counterfactual Thinking)**: "만약 ~했다면?" 질문으로 메타인지 자극
- **선택 설계 (Choice Architecture)**: Small Wins로 자기효능감 향상, 점진적 난이도

## 현재 N10 구조

```json
{
  "learning_tutor": {
    "custom_learning_path": {
      "path_summary": "학습 경로 요약",
      "learning_materials": ["학습 자료"],
      "practice_steps": ["실행 단계"],
      "recommended_topics": ["추천 주제"]
    },
    "investment_advisor": {
      "advisor_message": "공감 기반 조언 문구",
      "recommended_questions": ["행동 유도 질문"]
    },
    "action_missions": [ActionMission],
    "uncertainty_level": "low|medium|high"
  }
}
```

**현재 문제점:**
- 단순 정보 나열, 실행 확률 낮음
- "확증 편향을 조심하세요" → 추상적 조언
- 손실을 부정적으로만 표현
- 미션 난이도 무작위, 진행 상황 추적 없음

---

## 새로운 구조 (행동경제학 기반)

```json
{
  "learning_tutor": {
    "custom_learning_path": { ... },  // 기존 유지
    "investment_advisor": { ... },     // 기존 유지

    // ★ 신규: 프레이밍 효과
    "learning_frame": {
      "loss_reframe": {
        "original": "15.3% 손실",
        "reframed": "15.3%의 수업료를 내고 확증 편향이라는 교훈을 배웠습니다",
        "learning_value": "이 경험의 가치는 향후 더 큰 손실을 예방하는 데 있습니다"
      },
      "mistake_reframe": {
        "original": "뉴스만 보고 급하게 매수함",
        "reframed": "빠른 정보 반응력이라는 강점이 있으나, 검증 단계 추가 필요",
        "strength_focus": "빠른 정보 반응력"
      },
      "progress_frame": {
        "message": "자신의 패턴을 인식한 것 자체가 성장입니다",
        "comparison_anchor": "대부분의 투자자는 자신의 편향을 인식하지 못합니다"
      }
    },

    // ★ 신규: 사후 가정 사고
    "counterfactual_scenarios": [
      {
        "scenario_id": "CF001",
        "type": "upward",  // 더 좋은 결과 가정
        "question": "만약 매수 전에 반대 의견을 3개 찾아봤다면?",
        "alternative_action": "급등 뉴스의 신뢰성을 검증했을 것",
        "probable_outcome": "과열 신호 인지, 매수 시점 조절 가능",
        "lesson": "정보 검증 단계의 중요성",
        "emotion_target": "후회를 학습으로 전환"
      },
      {
        "scenario_id": "CF002",
        "type": "downward",  // 더 나쁜 결과 가정
        "question": "만약 손절 없이 계속 보유했다면?",
        "probable_outcome": "현재보다 2배 이상의 손실 가능성",
        "lesson": "손절 결정이 더 큰 손실을 방지함"
      },
      {
        "scenario_id": "CF003",
        "type": "neutral",  // 시장 변수 가정
        "question": "만약 시장이 반대로 움직였다면?",
        "lesson": "결과가 좋았어도 프로세스 문제는 동일함 (결과 편향 인식)"
      }
    ],

    // ★ 신규: 선택 설계
    "mission_architecture": {
      "onboarding_mission": {
        "mission_id": "M000",
        "type": "small_win",
        "title": "투자 일지 한 줄 적기",
        "description": "이 분석을 보고 느낀 점을 딱 한 줄만 적어보세요",
        "estimated_time": "30초",
        "completion_reward": "자기 인식의 첫 걸음을 뗐어요!"
      },
      "progression_path": [
        { "level": 1, "theme": "인식(Awareness)", "missions": ["M001"] },
        { "level": 2, "theme": "준비(Preparation)", "missions": ["M002", "M003"] },
        { "level": 3, "theme": "실행(Execution)", "missions": ["M004"] }
      ],
      "nudge_messages": {
        "first_visit": "가장 쉬운 것부터 시작해볼까요?",
        "return_visit": "지난번 미션은 어떠셨어요?",
        "long_absence": "작은 한 걸음이 큰 변화를 만들어요"
      }
    },

    // ★ 확장: 구현 의도 포함
    "action_missions": [
      {
        "mission_id": "M001",
        "priority": 1,
        "level": 1,
        "prerequisite": "M000",
        "title": "반대 의견 1개만 찾기",  // 난이도 낮춤 (3개 → 1개)
        "description": "매수하려는 종목의 부정적 의견을 딱 1개만 찾아보세요",
        "estimated_time": "5분",

        // ★ 신규: If-Then 플랜
        "if_then_plan": {
          "trigger_situation": "매수 버튼을 누르기 직전",
          "trigger_emotion": "확신이 들 때",
          "then_action": "검색창에 '[종목명] 리스크'를 검색한다",
          "commitment_phrase": "만약 매수 버튼을 누르려 할 때, 그때는 반드시 리스크 1개를 먼저 검색한다"
        },

        "behavioral_target": "확증 편향 완화",
        "expected_outcome": "정보 균형의 중요성 체감",
        "difficulty": "easy",  // 첫 미션은 쉽게
        "estimated_impact": "medium",
        "completion_reward": "균형 잡힌 시각의 첫 걸음!",
        "level_up_preview": "다음 단계: 반대 의견 3개 찾기"
      }
    ],

    "uncertainty_level": "low|medium|high"
  }
}
```

---

## 수정 대상 파일

### 1. N10_Learning_Tutor/prompt.py

- `NODE10_REPORT_PROMPT` 전면 개편
- 4가지 행동경제학 이론 적용 가이드라인 추가
- If-Then 플랜 작성 규칙 명시
- 편향별 트리거-행동 매핑 제공

### 2. N10_Learning_Tutor/n10.py

- `_normalize_learning_tutor()` 함수에 새 필드 처리 추가
- `_generate_fallback_missions()` 함수에 if_then_plan 추가
- 폴백 시나리오 생성 로직 추가

### 3. utils/validator.py

- `validate_action_missions()` 함수에 if_then_plan 검증 추가
- `validate_learning_frame()` 신규 함수 추가
- `validate_counterfactual_scenarios()` 신규 함수 추가

---

## 이론별 필드 매핑

### 1. 구현 의도 → `if_then_plan` (ActionMission 내부)

| 필드 | 타입 | 설명 |
|------|------|------|
| trigger_situation | string | 트리거 상황 (예: "매수 버튼을 누르기 직전") |
| trigger_emotion | string | 동반 감정 (예: "확신이 들 때") |
| then_action | string | 실행 행동 (예: "'[종목명] 리스크' 검색") |
| commitment_phrase | string | "만약 ~할 때, 그때는 ~한다" 형태 서약문 |

**편향별 If-Then 템플릿:**

| 편향 | If (상황) | Then (행동) |
|------|-----------|-------------|
| confirmation_bias | 매수 확신이 들 때 | 반대 의견 1개 검색 |
| loss_aversion | 손실률 -5% 도달 시 | 매매 일지 다시 읽기 |
| fomo | 급등 뉴스 발견 시 | 24시간 타이머 설정 |
| herding_effect | "다들 산다" 생각 시 | 나만의 분석 1문장 작성 |
| anchoring_effect | 전고점 비교 시 | 현재 PER/PBR 확인 |
| overconfidence | "내 감이 맞다" 생각 시 | 최근 투자 1건 결과 확인 |

---

### 2. 프레이밍 효과 → `learning_frame`

| 필드 | 설명 |
|------|------|
| loss_reframe.original | 원래 표현 (예: "15.3% 손실") |
| loss_reframe.reframed | 재정의 (예: "15.3% 수업료로 확증 편향 교훈 학습") |
| loss_reframe.learning_value | 이 경험의 가치 |
| mistake_reframe.strength_focus | 강점 포인트 (예: "빠른 정보 반응력") |
| progress_frame.comparison_anchor | 비교 기준 (예: "대부분의 투자자 대비") |

---

### 3. 사후 가정 사고 → `counterfactual_scenarios`

| 타입 | 목적 | 예시 질문 |
|------|------|----------|
| upward | 개선점 도출 | "만약 반대 의견을 찾았다면?" |
| downward | 현재 결정 긍정화 | "만약 손절 없이 보유했다면?" |
| neutral | 결과 편향 인식 | "만약 시장이 반대로 갔다면?" |

---

### 4. 선택 설계 → `mission_architecture`

| 필드 | 설명 |
|------|------|
| onboarding_mission | 30초 내 완료 가능한 Small Win 미션 |
| progression_path | 레벨 1~3 (인식→준비→실행) |
| nudge_messages | 방문 유형별 넛지 메시지 |

---

## 프론트엔드 신규 컴포넌트

```
frontend/components/N10/
├── IfThenPlanCard.tsx       # If-Then 플랜 시각화
├── LearningFrameCard.tsx    # 프레이밍 리프레임 카드
├── CounterfactualCard.tsx   # 사후 가정 시나리오 탭
├── MissionProgressCard.tsx  # 미션 진행률 + 레벨 맵
├── OnboardingMission.tsx    # Small Win 첫 미션
└── index.ts                 # 배럴 파일
```

### IfThenPlanCard UI 설계

```
┌─────────────────────────────────────────────────┐
│  🧠  나의 행동 서약                              │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─ IF ────────────────────────────────────────┐│
│  │  상황: 매수 버튼을 누르기 직전               ││
│  │  감정: 확신이 들 때                         ││
│  └─────────────────────────────────────────────┘│
│                     ↓                           │
│  ┌─ THEN ──────────────────────────────────────┐│
│  │  행동: '[종목명] 리스크' 검색               ││
│  └─────────────────────────────────────────────┘│
│                                                 │
│  ┌─ 서약문 ────────────────────────────────────┐│
│  │  "만약 매수 버튼을 누르려 할 때,            ││
│  │   그때는 반드시 리스크 1개를 먼저 검색한다"  ││
│  └─────────────────────────────────────────────┘│
└─────────────────────────────────────────────────┘
```

### LearningFrameCard UI 설계

```
┌─────────────────────────────────────────────────┐
│  🔄  시각의 전환                                │
├─────────────────────────────────────────────────┤
│                                                 │
│  ❌ 기존 시각                                   │
│  "15.3% 손실"  ← 취소선                         │
│                                                 │
│  ✓ 새로운 시각                                  │
│  "15.3%의 수업료를 내고 확증 편향이라는         │
│   중요한 교훈을 배웠습니다"                     │
│                                                 │
│  ┌─────────────────────────────────────────────┐│
│  │ 💪 발견된 강점: 빠른 정보 반응력             ││
│  └─────────────────────────────────────────────┘│
│                                                 │
│  "자신의 패턴을 인식한 것 자체가 성장입니다"    │
└─────────────────────────────────────────────────┘
```

### MissionProgressCard UI 설계

```
┌─────────────────────────────────────────────────┐
│  🎯  나의 성장 경로                              │
├─────────────────────────────────────────────────┤
│                                                 │
│  진행률: ████░░░░░░  40%                        │
│                                                 │
│  ┌───┐    ┌───┐    ┌───┐                       │
│  │ 1 │ →  │ 2 │ →  │ 3 │                       │
│  │ ✓ │    │ ● │    │   │                       │
│  └───┘    └───┘    └───┘                       │
│  인식     준비      실행                         │
│                                                 │
│  "가장 쉬운 것부터 시작해볼까요?"                │
└─────────────────────────────────────────────────┘
```

---

## 타입 정의 (frontend/types.ts)

```typescript
// 신규 타입 추가
export interface IfThenPlan {
  trigger_situation: string;
  trigger_emotion: string;
  then_action: string;
  commitment_phrase: string;
}

export interface LearningFrame {
  loss_reframe: {
    original: string;
    reframed: string;
    learning_value: string;
  };
  mistake_reframe: {
    original: string;
    reframed: string;
    strength_focus: string;
  };
  progress_frame: {
    message: string;
    comparison_anchor: string;
  };
}

export interface CounterfactualScenario {
  scenario_id: string;
  type: 'upward' | 'downward' | 'neutral';
  question: string;
  alternative_action?: string;
  probable_outcome: string;
  lesson: string;
  emotion_target?: string;
}

export interface MissionArchitecture {
  onboarding_mission: {
    mission_id: string;
    type: 'small_win';
    title: string;
    description: string;
    estimated_time: string;
    completion_reward: string;
  };
  progression_path: {
    level: number;
    theme: string;
    missions: string[];
    completion_criteria?: string;
    level_reward?: string;
  }[];
  nudge_messages: {
    first_visit: string;
    return_visit: string;
    long_absence: string;
  };
}

// ActionMission 확장
export interface ActionMissionV2 extends ActionMission {
  level?: number;
  prerequisite?: string | null;
  estimated_time?: string;
  if_then_plan?: IfThenPlan;
  completion_reward?: string;
  level_up_preview?: string | null;
}

// N10LearningTutor 확장
export interface N10LearningTutorV2 extends N10LearningTutor {
  learning_frame?: LearningFrame;
  counterfactual_scenarios?: CounterfactualScenario[];
  mission_architecture?: MissionArchitecture;
}
```

---

## 검증 방법

1. **단위 테스트**: `validate_action_missions()` 함수에 if_then_plan 포함 데이터 입력
2. **LLM 출력 검증**: `node10_learning_tutor()` 호출 후 JSON 구조 확인
3. **프론트엔드 연동**: 새 컴포넌트들이 정상 렌더링되는지 확인
4. **미션 진행 상태**: localStorage에 완료 미션 저장 및 로드 테스트

---

## 프론트엔드 고도화 전략

### 디자인 톤앤매너 유지 원칙

**현재 디자인 시스템 분석:**

- **배경**: `bg-slate-950` (다크 테마), `bg-slate-900/40` (카드)
- **N9 섹션 색상**: Emerald (`border-emerald-500/20`, `text-emerald-400`)
- **카드 스타일**: `rounded-3xl`, 반투명 배경 + 미묘한 border
- **타이포**: Inter + Noto Sans KR, 작은 사이즈(`text-[11px]`), `font-black` 헤드라인
- **애니메이션**: `fade-in`, `slide-in-from-bottom-4`

**유지해야 할 핵심 요소:**

1. Emerald 색상 팔레트 (N9 정체성)
2. 반투명 카드 + 점 그리드 배경 패턴
3. 확장/축소 인터랙션 패턴
4. 작고 밀도 높은 정보 표현 방식

---

### 새로운 UI 컴포넌트 설계

#### 1. 투자자 페르소나 카드 (investor_character)

```
┌─────────────────────────────────────────────────┐
│  🧠  나의 투자 성향                              │
├─────────────────────────────────────────────────┤
│                                                 │
│     ┌───────────────────────┐                   │
│     │   확증 편향형 수집가    │  ← 캐릭터 타입    │
│     └───────────────────────┘                   │
│                                                 │
│  "다양한 정보를 수집하지만, 자신의 기존 믿음을    │
│   확인하는 정보에 더 큰 가중치를 두는 경향"       │
│                                                 │
│  ┌─────────────────────────────┐                │
│  │ #confirmation_bias          │ ← 배지        │
│  └─────────────────────────────┘                │
└─────────────────────────────────────────────────┘
```

**스타일링:**

```tsx
// 캐릭터 타입 - 강조된 라벨 스타일
className="inline-block px-4 py-2 bg-emerald-500/20 border border-emerald-500/40
  rounded-2xl text-emerald-300 text-lg font-black tracking-tight"

// 설명 텍스트
className="text-[12px] text-slate-300 leading-relaxed mt-3"

// 편향 배지
className="inline-flex items-center gap-1.5 px-2.5 py-1 bg-slate-800/60
  rounded-full text-[10px] text-emerald-400/80 font-mono"
```

---

#### 2. 레이더 차트 (profile_metrics) - 모바일 최적화

**차트 라이브러리 없이 SVG + CSS로 구현** (기존 시스템 일관성 유지)

**모바일 환경 고려사항:**

- 차트 영역을 넓게 (w-48 → w-64, 전체 너비 활용)
- 터치 영역 충분히 확보
- 점수와 편향의 **역상관 관계**: 점수가 낮을수록 해당 편향이 강함

```
                    정보 민감도
                       85
                        ▲
                       ╱ ╲
      학습 적응력     ╱   ╲     분석 깊이
           70       ╱     ╲       45
                   ╱   ⚠️  ╲      ← 움푹 들어간 부분에 경고
                   ╲       ╱
                    ╲     ╱
          감정 통제   ╲   ╱   결단력
              40  🔴  ╲ ╱      55
                       ▼
                  리스크 관리
                     30 🔴    ← 취약점 강조
```

**구현 방식:**

```tsx
// SVG 기반 육각형 레이더 차트 (모바일 최적화)
<svg viewBox="0 0 240 240" className="w-64 h-64 mx-auto">
  {/* 배경 육각형 그리드 (20%, 40%, 60%, 80%, 100%) */}
  {[0.2, 0.4, 0.6, 0.8, 1].map(scale => (
    <polygon
      key={scale}
      points={hexagonPoints(scale)}
      className="fill-none stroke-slate-700/50 stroke-[0.5]"
    />
  ))}

  {/* 취약 구간 강조 (40점 이하 영역) */}
  <polygon
    points={weakZonePoints}
    className="fill-rose-500/10 stroke-none"
  />

  {/* 데이터 영역 */}
  <polygon
    points={dataPoints}
    className="fill-emerald-500/20 stroke-emerald-400 stroke-2"
  />

  {/* 각 축의 데이터 포인트 */}
  {metrics.map((m, i) => (
    <g key={m.key}>
      {/* 점수 원형 마커 */}
      <circle
        cx={pointPositions[i].x}
        cy={pointPositions[i].y}
        r={m.score <= 40 ? 8 : 5}
        className={m.score <= 40
          ? 'fill-rose-500 animate-pulse'
          : 'fill-emerald-400'}
      />

      {/* 취약점 경고 아이콘 (40점 이하) */}
      {m.score <= 40 && (
        <text
          x={warningPositions[i].x}
          y={warningPositions[i].y}
          className="text-[14px]"
        >
          ⚠️
        </text>
      )}
    </g>
  ))}

  {/* 각 축 라벨 + 점수 */}
  {metrics.map((m, i) => (
    <g key={`label-${m.key}`}>
      <text
        x={labelPositions[i].x}
        y={labelPositions[i].y}
        className={`text-[10px] font-medium text-center
          ${m.score <= 40 ? 'fill-rose-400' : 'fill-slate-400'}`}
      >
        {m.label}
      </text>
      <text
        x={labelPositions[i].x}
        y={labelPositions[i].y + 12}
        className={`text-[11px] font-bold
          ${m.score <= 40 ? 'fill-rose-400' : 'fill-emerald-400'}`}
      >
        {m.score}
      </text>
    </g>
  ))}
</svg>
```

**취약점 시각적 강조 시스템:**

| 점수 범위 | 시각적 표현 | 의미 |
|-----------|------------|------|
| 0~40 | 🔴 빨간 펄스 원 + ⚠️ 경고 아이콘 | **취약 영역** - 해당 편향이 강함 |
| 41~60 | 🟡 노란 원 | 주의 필요 |
| 61~100 | 🟢 초록 원 | 양호 |

**불균형 시각화:**

- 점수 40 이하: 🔴 빨간 펄스 애니메이션 + ⚠️ 경고 아이콘
- 점수 41~60: 🟡 노란색 표시
- 점수 61 이상: 🟢 초록색 (정상)
- 전체적으로 비대칭한 육각형 → 찌그러진 모양으로 사용자의 불균형 패턴 직관적 인지

**차트 하단 범례:**

```tsx
<div className="flex justify-center gap-4 mt-3 text-[10px]">
  <span className="flex items-center gap-1">
    <span className="w-2 h-2 rounded-full bg-rose-500" /> 취약
  </span>
  <span className="flex items-center gap-1">
    <span className="w-2 h-2 rounded-full bg-amber-500" /> 주의
  </span>
  <span className="flex items-center gap-1">
    <span className="w-2 h-2 rounded-full bg-emerald-500" /> 양호
  </span>
</div>
```

---

#### 3. 인지 편향 분석 카드 (cognitive_analysis)

```
┌─────────────────────────────────────────────────┐
│  ⚠️  감지된 인지 편향                            │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─ 주요 편향 ─────────────────────────────────┐│
│  │                                             ││
│  │  확증 편향  Confirmation Bias               ││
│  │  ─────────────────────────────────          ││
│  │  자신이 믿고 싶은 정보만 선택적으로 수용하고,  ││
│  │  반대 증거는 무시하는 경향                    ││
│  │                                             ││
│  │  💥 영향: 잘못된 투자 판단을 정당화하며       ││
│  │          손실을 키울 수 있습니다              ││
│  └─────────────────────────────────────────────┘│
│                                                 │
│  ┌─ 보조 편향 ─────────────────────────────────┐│
│  │  · 후견 편향 (Hindsight Bias)               ││
│  │    결과를 알고 난 후 '그럴 줄 알았다'고 착각  ││
│  └─────────────────────────────────────────────┘│
└─────────────────────────────────────────────────┘
```

**스타일링:**

```tsx
// 주요 편향 카드 - 강조 스타일
className="bg-amber-500/10 border border-amber-500/30 rounded-2xl p-4"

// 편향 이름 (한글 + 영문)
<span className="text-amber-300 font-bold text-sm">{name}</span>
<span className="text-amber-400/60 text-[10px] font-mono ml-2">{english}</span>

// 보조 편향 - 리스트 스타일
className="bg-slate-800/40 border border-slate-700/50 rounded-xl p-3 mt-3"
```

---

#### 4. 의사결정 문제점 카드 (decision_problems)

```
┌─────────────────────────────────────────────────┐
│  🎯  의사결정 패턴 분석                          │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─────────────────────────────────────────────┐│
│  │  뇌동매매                     빈도: ●●●     ││
│  │  ─────────────────────────────────          ││
│  │                                             ││
│  │  😰 심리적 트리거                            ││
│  │  FOMO (Fear Of Missing Out)                 ││
│  │                                             ││
│  │  📍 발생 상황                                ││
│  │  급등 뉴스를 접했을 때                        ││
│  │                                             ││
│  │  💭 그 순간의 생각                            ││
│  │  "지금 안 사면 영원히 기회를 놓친다"          ││
│  │                                             ││
│  │  ⚡ 결과                                     ││
│  │  고점 매수 후 하락 시 큰 손실                 ││
│  └─────────────────────────────────────────────┘│
└─────────────────────────────────────────────────┘
```

**빈도 표시 (frequency):**

```tsx
// 빈도 인디케이터
const FrequencyIndicator = ({ level }: { level: 'low' | 'medium' | 'high' }) => (
  <div className="flex gap-0.5">
    {[0, 1, 2].map(i => (
      <div
        key={i}
        className={`w-2 h-2 rounded-full ${
          i < { low: 1, medium: 2, high: 3 }[level]
            ? 'bg-rose-400'
            : 'bg-slate-700'
        }`}
      />
    ))}
  </div>
)
```

---

#### 5. 액션 미션 카드 (action_missions)

```
┌─────────────────────────────────────────────────┐
│  🚀  오늘의 투자 미션                            │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌─ M001 ──────────────────────────────────────┐│
│  │                                             ││
│  │  #1 우선순위                                 ││
│  │                                             ││
│  │  📋 반대 의견 3개 찾기                        ││
│  │  ─────────────────────────────────          ││
│  │  매수하려는 종목에 대해 부정적인 의견이나      ││
│  │  리스크 요인을 3가지 이상 찾아 기록하세요.     ││
│  │                                             ││
│  │  ┌─────────────────────────────────────┐    ││
│  │  │ 🎯 목표: 확증 편향 완화               │    ││
│  │  │ ✨ 기대효과: 균형 잡힌 정보 수집      │    ││
│  │  └─────────────────────────────────────┘    ││
│  │                                             ││
│  │  ┌──────────┐  ┌──────────┐                 ││
│  │  │ 난이도 중 │  │ 효과 높음 │                 ││
│  │  └──────────┘  └──────────┘                 ││
│  │                                             ││
│  │  [ ✓ 미션 시작하기 ]                         ││
│  └─────────────────────────────────────────────┘│
└─────────────────────────────────────────────────┘
```

**난이도/효과 배지:**

```tsx
const DifficultyBadge = ({ level }: { level: 'easy' | 'medium' | 'hard' }) => {
  const styles = {
    easy: 'bg-green-500/20 text-green-400 border-green-500/30',
    medium: 'bg-amber-500/20 text-amber-400 border-amber-500/30',
    hard: 'bg-rose-500/20 text-rose-400 border-rose-500/30'
  }
  const labels = { easy: '쉬움', medium: '보통', hard: '어려움' }

  return (
    <span className={`px-2 py-0.5 rounded-full text-[9px] font-bold border ${styles[level]}`}>
      난이도 {labels[level]}
    </span>
  )
}
```

---

### 수정 대상 프론트엔드 파일

| 파일 | 수정 내용 |
|------|----------|
| frontend/types.ts | `N9LearningPatternResponse` 타입 전면 개편 |
| frontend/components/AnalysisView.tsx | N9 섹션 (라인 454-648) 전면 개편 |
| frontend/components/constants.tsx | N9 관련 아이콘/메타데이터 추가 |

---

### 새로 추가할 컴포넌트

```
frontend/components/
├── N9/
│   ├── InvestorPersonaCard.tsx    # 투자자 캐릭터 카드
│   ├── RadarChart.tsx             # SVG 기반 레이더 차트
│   ├── CognitiveBiasCard.tsx      # 인지 편향 분석 카드
│   ├── DecisionProblemCard.tsx    # 의사결정 문제점 카드
│   ├── ActionMissionCard.tsx      # 액션 미션 카드
│   └── index.ts                   # 배럴 파일
```

---

### 레이아웃 구조 (모바일 - 세로 스택)

```
┌───────────────────────────────────┐
│  투자 패턴 분석 (N9)      [확장 ▼] │
├───────────────────────────────────┤
│                                   │
│  ┌───────────────────────────────┐│
│  │   🧠 나의 투자 성향            ││
│  │   ─────────────────────────   ││
│  │   확증 편향형 수집가            ││
│  │   "다양한 정보를 수집하지만..." ││
│  │   #confirmation_bias          ││
│  └───────────────────────────────┘│
│                                   │
│  ┌───────────────────────────────┐│
│  │         📊 투자 성향 분석      ││
│  │                               ││
│  │           정보 민감도          ││
│  │              85               ││
│  │               ▲               ││
│  │              ╱ ╲              ││
│  │    학습    ╱   ╲   분석       ││
│  │     70   ╱  ⚠️  ╲   45       ││
│  │          ╲     ╱              ││
│  │    감정   ╲   ╱  결단         ││
│  │    40 🔴  ╲ ╱    55          ││
│  │            ▼                  ││
│  │        리스크 관리             ││
│  │          30 🔴                ││
│  │                               ││
│  │   ● 취약  ● 주의  ● 양호      ││
│  └───────────────────────────────┘│
│                                   │
│  ┌───────────────────────────────┐│
│  │  ⚠️ 감지된 인지 편향           ││
│  │  ───────────────────────────  ││
│  │  주요: 확증 편향               ││
│  │  보조: 후견 편향               ││
│  └───────────────────────────────┘│
│                                   │
│  ┌───────────────────────────────┐│
│  │  🎯 의사결정 문제점            ││
│  │  ───────────────────────────  ││
│  │  뇌동매매 · FOMO              ││
│  └───────────────────────────────┘│
│                                   │
│  ┌───────────────────────────────┐│
│  │  🚀 오늘의 투자 미션           ││
│  │  ───────────────────────────  ││
│  │  반대 의견 3개 찾기            ││
│  │  [ 시작하기 ]                 ││
│  └───────────────────────────────┘│
│                                   │
└───────────────────────────────────┘
```

**모바일 레이아웃 CSS:**

```tsx
// 세로 스택 레이아웃
<div className="flex flex-col gap-4">
  <InvestorPersonaCard />
  <RadarChart />
  <CognitiveBiasCard />
  <DecisionProblemCard />
  <ActionMissionCard />
</div>
```

---

### 애니메이션 & 인터랙션

**1. 레이더 차트 애니메이션:**

```tsx
// 초기 로드 시 중심에서 확장
<polygon
  className="animate-[radar-expand_0.8s_ease-out_forwards]"
  style={{ transformOrigin: 'center' }}
/>

// @keyframes radar-expand
// from { transform: scale(0); opacity: 0; }
// to { transform: scale(1); opacity: 1; }
```

**2. 미션 카드 체크 애니메이션:**

```tsx
// 완료 시 체크마크 + 색상 변경
className={`transition-all duration-300 ${
  completed
    ? 'bg-emerald-500/20 border-emerald-500/40'
    : 'bg-slate-900/40 border-slate-700/50'
}`}
```

**3. 편향 탐지 시 하이라이트:**

```tsx
// bias_detected가 있는 축은 펄스 효과
<circle
  className={bias ? 'animate-pulse' : ''}
  fill={bias ? '#f59e0b' : '#10b981'}
/>
```

---

## 구현 범위: 핵심 기능 (2주)

**포함**: If-Then 플랜 + 프레이밍 효과

**추후 확장**: 사후 가정 사고 + 선택 설계(레벨 시스템)

---

## 구현 순서

### Phase 1: 백엔드 (1주)

#### 1. N10_Learning_Tutor/prompt.py

- NODE10_REPORT_PROMPT에 2가지 이론 적용:
  - **구현 의도**: If-Then 플랜 작성 가이드라인
  - **프레이밍 효과**: 손실→수업료, 실수→강점 재정의 규칙
- 편향별 If-Then 템플릿 제공

#### 2. N10_Learning_Tutor/n10.py

- `_normalize_learning_tutor()` 함수 수정:
  - `learning_frame` 필드 처리 추가
  - `action_missions[].if_then_plan` 필드 처리 추가
- `_generate_fallback_missions()` 함수 수정:
  - 편향별 if_then_plan 기본값 추가
- `_generate_fallback_frame()` 함수 신규:
  - N8 손실 원인 + N9 편향 기반 기본 프레임 생성

#### 3. utils/validator.py

- `validate_if_then_plan()` 함수 신규 (optional 검증)
- `validate_learning_frame()` 함수 신규 (optional 검증)

### Phase 2: 프론트엔드 (1주)

#### 4. frontend/types.ts

```typescript
// 신규 추가
export interface IfThenPlan {
  trigger_situation: string;
  trigger_emotion: string;
  then_action: string;
  commitment_phrase: string;
}

export interface LearningFrame {
  loss_reframe: { original: string; reframed: string; learning_value: string; };
  mistake_reframe: { original: string; reframed: string; strength_focus: string; };
  progress_frame: { message: string; comparison_anchor: string; };
}

// ActionMission 확장
export interface ActionMission {
  // ... 기존 필드
  if_then_plan?: IfThenPlan;  // 신규
}

// N10LearningTutor 확장
export interface N10LearningTutor {
  // ... 기존 필드
  learning_frame?: LearningFrame;  // 신규
}
```

#### 5. N10 컴포넌트 생성 (frontend/components/N10/)

- `IfThenPlanCard.tsx` - If-Then 플랜 시각화
- `LearningFrameCard.tsx` - 프레이밍 리프레임 카드
- `index.ts` - 배럴 파일

#### 6. frontend/components/AnalysisView.tsx

- "맞춤형 투자 학습 경로" 섹션에 LearningFrameCard 추가
- ActionMissionCard 내부에 IfThenPlanCard 연동

### Phase 3: 테스트 (2일)

7. API 응답 → 프론트엔드 렌더링 테스트
8. 폴백 시나리오 동작 확인
9. 반응형 레이아웃 검증

---

## 추후 확장 (Phase 2)

- **사후 가정 사고**: `counterfactual_scenarios` 필드 + CounterfactualCard
- **선택 설계**: `mission_architecture` 필드 + MissionProgressCard + 레벨 시스템

---

## 주요 파일 요약

| 파일 | 수정 내용 |
|------|----------|
| N10_Learning_Tutor/prompt.py | 행동경제학 4대 이론 적용 프롬프트 |
| N10_Learning_Tutor/n10.py | 신규 필드 정규화 + 폴백 로직 |
| utils/validator.py | 신규 필드 검증 함수 추가 |
| frontend/types.ts | 신규 타입 정의 |
| frontend/components/N10/ | 5개 신규 컴포넌트 |
| frontend/components/AnalysisView.tsx | N10 섹션 고도화 |

---

## 하위 호환성

- 기존 `custom_learning_path`, `investment_advisor`, `action_missions` 필드는 **그대로 유지**
- 신규 필드(`learning_frame`, `counterfactual_scenarios`, `mission_architecture`, `if_then_plan`)는 **optional**
- 프론트엔드에서 신규 필드가 없어도 기존 UI는 정상 동작
- 폴백 로직으로 LLM이 신규 필드를 생성하지 못해도 기본값 제공
